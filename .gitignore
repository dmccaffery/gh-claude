# Local dev build of the extension binary
/gh-claude
# Precompiled release artifacts (gh-claude-<os>-<arch>[.exe])
/gh-claude-*
/dist/
# Test coverage + results (emitted by `make test`)
/coverage/

# Docs site (zensical build output)
/site/

# Python (zensical via uv)
/.venv/

# Node tooling
node_modules/
